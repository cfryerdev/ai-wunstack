/**
 * @swagger
 * definitions:
 *   ResponsePrompt:
 *     properties:
 *       prompt:
 *         type: string
 *       response:
 *         type: string
 */
export interface ResponsePrompt {
    prompt: string;
    response: string;
}